#!/bin/bash
set -e
set -v

# Start the Zensical dev server with live reload
# Run from the repo root: ./s/up
# Assumes .venv exists; create with: python3 -m venv .venv && .venv/bin/pip install -r requirements.txt

.venv/bin/zensical serve "$@"
